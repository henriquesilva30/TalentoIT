@model IEnumerable<TalentoIT.Entities.skill>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome_skill)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.area)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome_skill)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.area)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_skill">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_skill">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_skill">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
