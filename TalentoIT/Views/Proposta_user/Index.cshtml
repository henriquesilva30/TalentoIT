@model IEnumerable<TalentoIT.Entities.proposta_user>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id_propostaNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_userNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_propostaNavigation.descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_userNavigation.email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_proposta_user">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_proposta_user">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_proposta_user">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
